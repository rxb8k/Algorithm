// Gold IV
#include <iostream>
using namespace std;

// 서로 대각선이나 같은 행, 열에 있으면 공격 가능
unsigned int N; // 1~15

int main() {
    cin>>N;
    int** arr=new int*[N];
    for(int i=0;i<N;i++) arr[i]=new int[N];
    
}